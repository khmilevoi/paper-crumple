node_modules/
dist/
.turbo/
*.tsbuildinfo

# Specs and implementation plans stay local.
docs/superpowers/
coverage/
.claude/worktrees/
test-results/
playwright-report/
# Benchmark output (tools/bench/cpu writes cpu.json and .cpuprofile files here).
tools/bench/out/
.playwright-mcp
.vitest-attachments/

# Sound for the playground demo. The .wav files come from the throwaway spike the library was
# ported from and are not this repository's to redistribute, so the whole folder stays local:
# the demo probes for it, degrades to silence when it is absent, and says so in the inspector.
examples/playground/public/audio/

# The GL bench writes its JSON and CPU profiles here (`pnpm bench:gl`, BENCH_OUT).
tools/bench/out/
