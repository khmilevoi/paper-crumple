---
'@paper-crumple/core': minor
'@paper-crumple/react': minor
'@paper-crumple/reatom': minor
---

Add independent Reatom scene, view, resource, and run models with native async data/error state and opt-in animation progress. Share framework-free lifecycle and acquisition controllers through `@paper-crumple/core/bindings`, and let React render the minimal view contract without owning the Reatom scene.

Keep semantic subscriptions off the animation frame path, preserve the TypeScript 5.0 declaration floor, and validate all five published packages and adapter import independence.
